<div className={VS`VSS(head.stylesheet, "lens-selector")`}>
  <div
      className={VS`VSS(head.stylesheet, "lens-selector-button")`}
      onClick={VS`head.lensSelectionFactory("LensTargetScripter")`}>
    Structure and scripts
  </div>
  <div
      className={VS`VSS(head.stylesheet, "lens-selector-button")`}
      onClick={VS`head.lensSelectionFactory("LensTargetDocumenter")`}>
    Documentation
  </div>
</div>